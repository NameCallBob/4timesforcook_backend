[pytest]
DJANGO_SETTINGS_MODULE = KBQA_Meibuy.settings
python_files = tests.py test_*.py *_tests.py
addopts = -q -p pytest_env_setup
# pytest-django forces DEBUG=False by default; keep it on so the
# password-reset endpoint returns its DEBUG-only uid/token to the tests.
django_debug_mode = true
